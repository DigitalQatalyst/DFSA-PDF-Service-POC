<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DFSA Authorised Individual Application</title>
  <style>
    /* DFSA Brand Colors */
    :root {
      --dfsa-primary-red: #B82933;
      --dfsa-gold-accent: #A39043;
      --dfsa-light-grey: #E5E5E5;
      --dfsa-dark-grey: #333333;
      --dfsa-white: #FFFFFF;
      --dfsa-text-primary: #1a1a1a;
      --dfsa-text-secondary: #666666;
    }

    /* Global Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      font-size: 11pt;
      line-height: 1.6;
      color: var(--dfsa-text-primary);
      background-color: var(--dfsa-white);
      padding: 0;
      margin: 0;
    }

    .container {
      max-width: 100%;
      margin: 0 auto;
      padding: 0;
    }

    /* Document Header */
    .document-header {
      background: linear-gradient(135deg, var(--dfsa-primary-red) 0%, #9a1f28 100%);
      color: var(--dfsa-white);
      padding: 30px 40px;
      margin-bottom: 30px;
      border-bottom: 4px solid var(--dfsa-gold-accent);
    }

    .document-header h1 {
      font-size: 24pt;
      font-weight: 700;
      margin-bottom: 10px;
      letter-spacing: 0.5px;
    }

    .document-header p {
      font-size: 11pt;
      opacity: 0.95;
      margin: 5px 0;
    }

    /* Section Styles */
    .section {
      margin-bottom: 30px;
      page-break-inside: avoid;
      background-color: var(--dfsa-white);
      border: 1px solid var(--dfsa-light-grey);
      border-radius: 6px;
      overflow: hidden;
    }

    .section-header {
      background-color: var(--dfsa-primary-red);
      color: var(--dfsa-white);
      padding: 12px 20px;
      font-size: 14pt;
      font-weight: 600;
      border-left: 5px solid var(--dfsa-gold-accent);
    }

    .section-content {
      padding: 20px 25px;
    }

    /* Field Styles */
    .field-group {
      margin-bottom: 18px;
      page-break-inside: avoid;
    }

    .field-label {
      font-weight: 600;
      color: var(--dfsa-dark-grey);
      margin-bottom: 6px;
      font-size: 10.5pt;
      display: block;
    }

    .field-value {
      color: var(--dfsa-text-primary);
      padding: 10px 15px;
      background-color: #f9f9f9;
      border-left: 3px solid var(--dfsa-gold-accent);
      border-radius: 3px;
      min-height: 32px;
      font-size: 11pt;
    }

    .field-value.empty {
      color: var(--dfsa-text-secondary);
      font-style: italic;
    }

    .field-value.boolean-yes {
      background-color: #e8f5e9;
      border-left-color: #4caf50;
      color: #2e7d32;
      font-weight: 600;
    }

    .field-value.boolean-no {
      background-color: #fce4ec;
      border-left-color: #e91e63;
      color: #c2185b;
      font-weight: 600;
    }

    /* Grid Layouts */
    .field-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
      margin-bottom: 20px;
    }

    .field-grid-3 {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 20px;
      margin-bottom: 20px;
    }

    .field-full-width {
      grid-column: 1 / -1;
    }

    /* Repeating Section Styles */
    .repeating-item {
      background-color: #fafafa;
      border: 1px solid var(--dfsa-light-grey);
      border-radius: 4px;
      padding: 20px;
      margin-bottom: 20px;
      position: relative;
    }

    .repeating-item::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: 4px;
      background-color: var(--dfsa-gold-accent);
    }

    .repeating-item-header {
      font-weight: 600;
      color: var(--dfsa-primary-red);
      margin-bottom: 15px;
      font-size: 12pt;
      padding-bottom: 10px;
      border-bottom: 2px solid var(--dfsa-light-grey);
    }

    .item-divider {
      border: 0;
      height: 2px;
      background: linear-gradient(to right, var(--dfsa-light-grey), transparent);
      margin: 25px 0;
    }

    /* Conditional Sections */
    .conditional-section {
      background-color: #fffef5;
      border-left: 4px solid var(--dfsa-gold-accent);
      padding: 20px;
      margin: 20px 0;
      border-radius: 4px;
    }

    .conditional-header {
      font-weight: 600;
      color: var(--dfsa-dark-grey);
      margin-bottom: 15px;
      font-size: 11.5pt;
    }

    /* Table Styles */
    table {
      width: 100%;
      border-collapse: collapse;
      margin: 15px 0;
    }

    th {
      background-color: var(--dfsa-primary-red);
      color: var(--dfsa-white);
      padding: 12px;
      text-align: left;
      font-weight: 600;
      font-size: 10.5pt;
    }

    td {
      padding: 10px 12px;
      border-bottom: 1px solid var(--dfsa-light-grey);
      font-size: 10.5pt;
    }

    tr:nth-child(even) {
      background-color: #f9f9f9;
    }

    tr:hover {
      background-color: #f0f0f0;
    }

    /* Document Footer */
    .document-footer {
      margin-top: 40px;
      padding: 25px 40px;
      background-color: var(--dfsa-light-grey);
      border-top: 3px solid var(--dfsa-primary-red);
      font-size: 9pt;
      color: var(--dfsa-text-secondary);
      text-align: center;
    }

    .document-footer p {
      margin: 5px 0;
    }

    /* Utility Classes */
    .text-center {
      text-align: center;
    }

    .text-bold {
      font-weight: 600;
    }

    .text-muted {
      color: var(--dfsa-text-secondary);
    }

    .mb-0 { margin-bottom: 0; }
    .mb-10 { margin-bottom: 10px; }
    .mb-20 { margin-bottom: 20px; }
    .mt-20 { margin-top: 20px; }

    /* Print Optimization */
    @media print {
      body {
        margin: 0;
        padding: 0;
      }

      .section {
        page-break-inside: avoid;
      }

      .repeating-item {
        page-break-inside: avoid;
      }
    }
  </style>
</head>
<body>
  <div class="container">

    <!-- Document Header -->
    <div class="document-header">
      <h1>AUTHORISED INDIVIDUAL APPLICATION</h1>
      <p><strong>Dubai Financial Services Authority (DFSA)</strong></p>
      <p>Application ID: {{Application.Id}}</p>
    </div>

    <!-- Section 1: Application Guidelines -->
    <div class="section">
      <div class="section-header">
        1. APPLICATION GUIDELINES
      </div>
      <div class="section-content">
        <div class="field-group">
          <span class="field-label">I confirm that I have carefully read and understood the guidelines:</span>
          <div class="field-value">
            {{defaultValue Guidelines.ConfirmRead "Not specified"}}
          </div>
        </div>
      </div>
    </div>

    <!-- Section 2: DIFC Disclosure -->
    <div class="section">
      <div class="section-header">
        2. DIFC DISCLOSURE
      </div>
      <div class="section-content">
        <div class="field-group">
          <span class="field-label">Do you consent to the disclosure of information in this application by the DFSA to the DIFCA?</span>
          {{#if DIFCDisclosure.ConsentToDisclosure}}
            <div class="field-value boolean-yes">Yes</div>
          {{else}}
            <div class="field-value boolean-no">No</div>
          {{/if}}
        </div>
      </div>
    </div>

    <!-- Section 3: Firm Information -->
    <div class="section">
      <div class="section-header">
        3. FIRM INFORMATION
      </div>
      <div class="section-content">
        <div class="field-grid">
          <div class="field-group">
            <span class="field-label">Firm Name:</span>
            <div class="field-value">
              {{defaultValue Application.FirmName "-"}}
            </div>
          </div>
          <div class="field-group">
            <span class="field-label">Firm Number:</span>
            <div class="field-value">
              {{defaultValue Application.FirmNumber "-"}}
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Section 4: Requestor Information -->
    <div class="section">
      <div class="section-header">
        4. REQUESTOR INFORMATION
      </div>
      <div class="section-content">
        <div class="field-grid">
          <div class="field-group">
            <span class="field-label">Name of person making the submission:</span>
            <div class="field-value">
              {{defaultValue Application.Requestor.Name "-"}}
            </div>
          </div>
          <div class="field-group">
            <span class="field-label">Position / Title:</span>
            <div class="field-value">
              {{defaultValue Application.Requestor.Position "-"}}
            </div>
          </div>
          <div class="field-group">
            <span class="field-label">Email address:</span>
            <div class="field-value">
              {{defaultValue Application.Requestor.Email "-"}}
            </div>
          </div>
          <div class="field-group">
            <span class="field-label">Contact Telephone Number:</span>
            <div class="field-value">
              {{defaultValue Application.Requestor.Phone "-"}}
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Section 5: Authorised Individual Information -->
    <div class="section">
      <div class="section-header">
        5. AUTHORISED INDIVIDUAL INFORMATION
      </div>
      <div class="section-content">
        <div class="field-group">
          <span class="field-label">What is the name of proposed Authorised Individual?</span>
          <div class="field-value">
            {{defaultValue Application.AuthorisedIndividualName "-"}}
          </div>
        </div>
      </div>
    </div>

    <!-- Section 6: Applicant Information -->
    <div class="section">
      <div class="section-header">
        6. APPLICANT INFORMATION
      </div>
      <div class="section-content">
        <div class="field-group">
          <span class="field-label">Is the Candidate applying on behalf of a Representative Office?</span>
          {{#if Flags.RepOffice}}
            <div class="field-value boolean-yes">Yes</div>
          {{else}}
            <div class="field-value boolean-no">No</div>
          {{/if}}
        </div>

        {{#if Flags.RepOffice}}
          <div class="conditional-section">
            <div class="conditional-header">Representative Office Functions</div>
            <div class="field-group">
              <span class="field-label">Please indicate the function(s) the Candidate is applying for within the Representative Office:</span>
              <div class="field-value">
                {{defaultValue Application.RepOfficeFunctions "-"}}
              </div>
            </div>
          </div>
        {{/if}}
      </div>
    </div>

    <!-- Section 7: Candidate Identity -->
    <div class="section">
      <div class="section-header">
        7. CANDIDATE IDENTITY
      </div>
      <div class="section-content">
        <div class="field-group">
          <span class="field-label">Has the candidate previously applied or held Authorised Individual status with the DFSA?</span>
          {{#if Flags.PreviouslyHeld}}
            <div class="field-value boolean-yes">Yes</div>
          {{else}}
            <div class="field-value boolean-no">No</div>
          {{/if}}
        </div>
      </div>
    </div>

    <!-- Section 8: Candidate Passport Details -->
    <div class="section">
      <div class="section-header">
        8. CANDIDATE PASSPORT DETAILS
      </div>
      <div class="section-content">
        {{#each PassportDetails}}
          <div class="repeating-item">
            <div class="repeating-item-header">Passport Details {{@index}}</div>

            <div class="field-grid-3">
              <div class="field-group">
                <span class="field-label">Title:</span>
                <div class="field-value">
                  {{defaultValue Title "-"}}
                </div>
              </div>
              <div class="field-group field-full-width">
                <span class="field-label">Full Name (as it appears in the principal passport):</span>
                <div class="field-value">
                  {{defaultValue FullName "-"}}
                </div>
              </div>
            </div>

            <div class="field-grid">
              <div class="field-group">
                <span class="field-label">Date of birth:</span>
                <div class="field-value">
                  {{formatDate DateOfBirth}}
                </div>
              </div>
              <div class="field-group">
                <span class="field-label">Place of birth:</span>
                <div class="field-value">
                  {{defaultValue PlaceOfBirth "-"}}
                </div>
              </div>
            </div>

            <div class="field-grid">
              <div class="field-group">
                <span class="field-label">UAE resident:</span>
                {{#if UaeResident}}
                  <div class="field-value boolean-yes">Yes</div>
                {{else}}
                  <div class="field-value boolean-no">No</div>
                {{/if}}
              </div>
              <div class="field-group">
                <span class="field-label">No. of citizenships:</span>
                <div class="field-value">
                  {{defaultValue NumberOfCitizenships "-"}}
                </div>
              </div>
            </div>

            <div class="field-grid">
              <div class="field-group">
                <span class="field-label">Other names:</span>
                <div class="field-value">
                  {{defaultValue OtherNames "-"}}
                </div>
              </div>
              <div class="field-group">
                <span class="field-label">Name in Native language:</span>
                <div class="field-value">
                  {{defaultValue NativeName "-"}}
                </div>
              </div>
            </div>
          </div>
        {{/each}}
      </div>
    </div>

    <!-- Section 9: Candidate Other Names (Conditional) -->
    {{#if Flags.OtherNames}}
    <div class="section">
      <div class="section-header">
        9. CANDIDATE OTHER NAMES
      </div>
      <div class="section-content">
        <div class="field-group mb-20">
          <span class="field-label">Has the candidate ever used other names or changed names?</span>
          <div class="field-value boolean-yes">Yes</div>
        </div>

        <div class="conditional-section">
          <div class="conditional-header">Other Names Details</div>

          <div class="field-group">
            <span class="field-label">State other names:</span>
            <div class="field-value">
              {{defaultValue Application.OtherNames.StateOtherNames "-"}}
            </div>
          </div>

          <div class="field-group">
            <span class="field-label">Name in native language (if applicable):</span>
            <div class="field-value">
              {{defaultValue Application.OtherNames.NativeName "-"}}
            </div>
          </div>

          <div class="field-grid">
            <div class="field-group">
              <span class="field-label">Date name changed:</span>
              <div class="field-value">
                {{formatDate Application.OtherNames.DateChanged}}
              </div>
            </div>
            <div class="field-group">
              <span class="field-label">Reason for change of name:</span>
              <div class="field-value">
                {{defaultValue Application.OtherNames.Reason "-"}}
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    {{else}}
    <div class="section">
      <div class="section-header">
        9. CANDIDATE OTHER NAMES
      </div>
      <div class="section-content">
        <div class="field-group">
          <span class="field-label">Has the candidate ever used other names or changed names?</span>
          <div class="field-value boolean-no">No</div>
        </div>
      </div>
    </div>
    {{/if}}

    <!-- Section 10: Candidate Citizenship -->
    <div class="section">
      <div class="section-header">
        10. CANDIDATE CITIZENSHIP
      </div>
      <div class="section-content">
        {{#each Citizenships}}
          <div class="repeating-item">
            <div class="repeating-item-header">Citizenship {{@index}}</div>

            <div class="field-grid-3">
              <div class="field-group">
                <span class="field-label">Country/Territory:</span>
                <div class="field-value">
                  {{defaultValue Country "-"}}
                </div>
              </div>
              <div class="field-group">
                <span class="field-label">Passport No.:</span>
                <div class="field-value">
                  {{defaultValue PassportNo "-"}}
                </div>
              </div>
              <div class="field-group">
                <span class="field-label">Expiry Date:</span>
                <div class="field-value">
                  {{formatDate ExpiryDate}}
                </div>
              </div>
            </div>
          </div>
        {{/each}}
      </div>
    </div>

    <!-- Section 11: Candidate Address and Contact -->
    <div class="section">
      <div class="section-header">
        11. CANDIDATE ADDRESS AND CONTACT
      </div>
      <div class="section-content">
        <div class="field-group">
          <span class="field-label">Address:</span>
          <div class="field-value">
            {{defaultValue Application.Contact.Address "-"}}
          </div>
        </div>

        <div class="field-grid">
          <div class="field-group">
            <span class="field-label">Postcode / PO box:</span>
            <div class="field-value">
              {{defaultValue Application.Contact.PostCode "-"}}
            </div>
          </div>
          <div class="field-group">
            <span class="field-label">Country:</span>
            <div class="field-value">
              {{defaultValue Application.Contact.Country "-"}}
            </div>
          </div>
        </div>

        <div class="field-grid">
          <div class="field-group">
            <span class="field-label">Mobile telephone number:</span>
            <div class="field-value">
              {{defaultValue Application.Contact.Mobile "-"}}
            </div>
          </div>
          <div class="field-group">
            <span class="field-label">Contact Email Address:</span>
            <div class="field-value">
              {{defaultValue Application.Contact.Email "-"}}
            </div>
          </div>
        </div>

        <div class="field-group">
          <span class="field-label">For how long has the candidate resided at the above address?</span>
          <div class="field-value">
            {{defaultValue Application.Contact.ResidenceDuration "-"}}
          </div>
        </div>
      </div>
    </div>

    <!-- Section 12: Previous Address (Conditional) -->
    {{#if Flags.ResidenceDurationLessThan3Years}}
    <div class="section">
      <div class="section-header">
        12. PREVIOUS ADDRESS
      </div>
      <div class="section-content">
        <div class="conditional-section">
          <div class="conditional-header">Previous Address Details (Residence less than 3 years)</div>

          <div class="field-group">
            <span class="field-label">Address:</span>
            <div class="field-value">
              {{defaultValue Application.PreviousAddress.Address "-"}}
            </div>
          </div>

          <div class="field-grid">
            <div class="field-group">
              <span class="field-label">Postcode / PO box:</span>
              <div class="field-value">
                {{defaultValue Application.PreviousAddress.PostCode "-"}}
              </div>
            </div>
            <div class="field-group">
              <span class="field-label">Country:</span>
              <div class="field-value">
                {{defaultValue Application.PreviousAddress.Country "-"}}
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    {{/if}}

    <!-- Section 13: Licensed Functions (Conditional) -->
    {{#if LicensedFunctions.ShowLicensedFunctionsSection}}
    <div class="section">
      <div class="section-header">
        13. LICENSED FUNCTIONS
      </div>
      <div class="section-content">
        <div class="field-group mb-20">
          <span class="field-label">Please select the Licensed Function to be carried out by the candidate:</span>
          <div class="field-value">
            {{defaultValue LicensedFunctions.LicensedFunctionChoiceLabel "-"}}
          </div>
        </div>

        {{#if LicensedFunctions.ShowMandatoryFunctionsQuestion}}
        <div class="conditional-section">
          <div class="conditional-header">Mandatory Licensed Functions</div>
          <p class="mb-20 text-muted">Please indicate if the candidate will also carry out any of the following mandatory Licensed Functions:</p>

          <div class="field-grid">
            <div class="field-group">
              <span class="field-label">Senior Executive Officer:</span>
              {{#if LicensedFunctions.SeniorExecutiveOfficer}}
                <div class="field-value boolean-yes">Yes</div>
              {{else}}
                <div class="field-value boolean-no">No</div>
              {{/if}}
            </div>
            <div class="field-group">
              <span class="field-label">Finance Officer:</span>
              {{#if LicensedFunctions.FinanceOfficer}}
                <div class="field-value boolean-yes">Yes</div>
              {{else}}
                <div class="field-value boolean-no">No</div>
              {{/if}}
            </div>
            <div class="field-group">
              <span class="field-label">Compliance Officer:</span>
              {{#if LicensedFunctions.ComplianceOfficer}}
                <div class="field-value boolean-yes">Yes</div>
              {{else}}
                <div class="field-value boolean-no">No</div>
              {{/if}}
            </div>
            <div class="field-group">
              <span class="field-label">Money Laundering Reporting Officer:</span>
              {{#if LicensedFunctions.MLRO}}
                <div class="field-value boolean-yes">Yes</div>
              {{else}}
                <div class="field-value boolean-no">No</div>
              {{/if}}
            </div>
          </div>
        </div>
        {{/if}}

        {{#if LicensedFunctions.ShowResponsibleOfficerConfirmations}}
        <div class="conditional-section mt-20">
          <div class="conditional-header">Responsible Officer Confirmations</div>

          <div class="field-group">
            <span class="field-label">The applicant has significant responsibility for the management of one or more aspects of an Authorised Firm's affairs:</span>
            <div class="field-value">
              {{defaultValue LicensedFunctions.RespResp1 "-"}}
            </div>
          </div>

          <div class="field-group">
            <span class="field-label">The applicant exercises a significant influence on the firm as a result of (a); and:</span>
            <div class="field-value">
              {{defaultValue LicensedFunctions.RespResp2 "-"}}
            </div>
          </div>

          <div class="field-group">
            <span class="field-label">The applicant is not an Employee of the Authorised Firm:</span>
            <div class="field-value">
              {{defaultValue LicensedFunctions.RespResp3 "-"}}
            </div>
          </div>
        </div>
        {{/if}}
      </div>
    </div>
    {{/if}}

    <!-- Section 14: Candidate Position -->
    <div class="section">
      <div class="section-header">
        14. CANDIDATE POSITION
      </div>
      <div class="section-content">
        <div class="field-group">
          <span class="field-label">What is the candidate's proposed job title?</span>
          <div class="field-value">
            {{defaultValue Position.ProposedJobTitle "-"}}
          </div>
        </div>

        <div class="field-group mb-20">
          <span class="field-label">Do you have proposed starting date?</span>
          {{#if Flags.HasStartDate}}
            <div class="field-value boolean-yes">Yes</div>
          {{else}}
            <div class="field-value boolean-no">No</div>
          {{/if}}
        </div>

        {{#if Flags.HasStartDate}}
        <div class="conditional-section">
          <div class="field-group">
            <span class="field-label">What is the candidate's proposed starting date?</span>
            <div class="field-value">
              {{formatDate Position.ProposedStartDate}}
            </div>
          </div>
        </div>
        {{else}}
        <div class="conditional-section">
          <div class="field-group">
            <span class="field-label">Please explain:</span>
            <div class="field-value">
              {{defaultValue Position.StartDateExplanation "-"}}
            </div>
          </div>
        </div>
        {{/if}}

        <div class="field-group mt-20">
          <span class="field-label">Does the candidate hold or has previously held, in a personal capacity, any licence, registration or equivalent with any Financial Services Regulator?</span>
          {{#if Flags.HasRegulatoryHistory}}
            <div class="field-value boolean-yes">Yes</div>
          {{else}}
            <div class="field-value boolean-no">No</div>
          {{/if}}
        </div>

        <div class="field-group">
          <span class="field-label">Will the candidate applying for Principal Representative also be appointed as the MLRO?</span>
          {{#if Position.WillBeMLRO}}
            <div class="field-value boolean-yes">Yes</div>
          {{else}}
            <div class="field-value boolean-no">No</div>
          {{/if}}
        </div>
      </div>
    </div>

    <!-- Section 15: Regulatory History (Conditional) -->
    {{#if Flags.HasRegulatoryHistory}}
    <div class="section">
      <div class="section-header">
        15. REGULATORY HISTORY
      </div>
      <div class="section-content">
        {{#each RegulatoryHistory}}
          <div class="repeating-item">
            <div class="repeating-item-header">Regulatory History Entry {{@index}}</div>

            <div class="field-group">
              <span class="field-label">Regulator:</span>
              <div class="field-value">
                {{defaultValue Regulator "-"}}
              </div>
            </div>

            <div class="field-grid">
              <div class="field-group">
                <span class="field-label">Date Started:</span>
                <div class="field-value">
                  {{formatDate DateStarted}}
                </div>
              </div>
              <div class="field-group">
                <span class="field-label">Date Finished (if applicable):</span>
                <div class="field-value">
                  {{formatDate DateFinished}}
                </div>
              </div>
            </div>

            <div class="field-grid">
              <div class="field-group">
                <span class="field-label">Name of license / registration:</span>
                <div class="field-value">
                  {{defaultValue LicenseName "-"}}
                </div>
              </div>
              <div class="field-group">
                <span class="field-label">Name of license register:</span>
                <div class="field-value">
                  {{defaultValue RegisterName "-"}}
                </div>
              </div>
            </div>

            <div class="field-group">
              <span class="field-label">Brief overview of license / registration:</span>
              <div class="field-value">
                {{defaultValue Overview "-"}}
              </div>
            </div>

            {{#if IsOtherRegulator}}
            <div class="conditional-section">
              <div class="field-group">
                <span class="field-label">Please provide details:</span>
                <div class="field-value">
                  {{defaultValue OtherRegulatorDetails "-"}}
                </div>
              </div>
            </div>
            {{/if}}
          </div>
        {{/each}}
      </div>
    </div>
    {{/if}}

    <!-- Document Footer -->
    <div class="document-footer">
      <p><strong>Document Generated:</strong> {{GeneratedAt}}</p>
      <p><strong>Template Version:</strong> {{TemplateVersion}}</p>
      <p style="margin-top: 15px; font-size: 8pt;">
        This is an official document generated by the DFSA PDF Generation Service.<br>
        Dubai Financial Services Authority | DIFC, Dubai, UAE
      </p>
    </div>

  </div>
</body>
</html>
